<local:WizardPage x:Class="DataAnalyzer.OutputParameterPicker"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:local="clr-namespace:DataAnalyzer"
                  mc:Ignorable="d"
                  d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <Binding RelativeSource="{RelativeSource Self}" />
    </UserControl.DataContext>
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Select a result parameter to evaluate:"
                       FontWeight="Bold"
                       Margin="0,0,0,10" />
            
            <ListBox ItemsSource="{Binding Parameters}" 
                     SelectedItem="{Binding SelectedParameter, Mode=TwoWay}"
                     BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Content="{Binding}" 
                                     IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected, Mode=TwoWay}"
                                     GroupName="ResultParameters"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</local:WizardPage>